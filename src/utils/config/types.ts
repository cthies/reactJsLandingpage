export type MainConfig = {
  appDomain: string;
  defaultLanguage: string;
  region: string;
  country: string;
  currency: string;
  currency_code: string;
  shipping_threshold: string;
  customer_service_page: string;
  current_store_id: string;
};
